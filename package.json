{
  "private": true,
  "name": "monorepo",
  "version": "0.0.0",
  "packageManager": "bun@1.3.11",
  "scripts": {
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "build": "turbo run build",
    "test": "turbo run test --",
    "test:e2e": "turbo run e2e",
    "dev": "turbo run dev",
    "serve": "turbo run serve",
    "set-version": "turbo run set-version --",
    "publish": "turbo run publish --"
  },
  "workspaces": [
    "apps/**",
    "packages/**",
    "e2e/**"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/node": "26.1.1",
    "@types/bun": "^1.3.12",
    "@vitest/coverage-v8": "^4.1.2",
    "tsdown": "0.22.14",
    "turbo": "^2.5.0",
    "typescript": "^6.0.0",
    "vitest": "^4.1.2",
    "vitest-when": "^0.10.0"
  }
}
